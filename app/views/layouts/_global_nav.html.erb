<div class="navbar-fixed">
  <nav>
      <div class="nav-wrapper">

          <%= link_to image_tag('logo-green.png') + 'Jamberri', root_path, class: "brand-logo" %>


        <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
          <% if user_signed_in? %>
            <li><%= link_to 'post an event', '#' %></li>
            <li><%= link_to current_user.username, profile_path(current_user.username) %></l1>
            <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to 'Join', new_user_registration_path %></li>
            <li><%= link_to 'Sign in ', new_user_session_path %></li>
          <% end %>
        </ul>

        <ul class="side-nav" id="mobile-demo">
          <% if user_signed_in? %>
            <li><%= link_to current_user.username, '#' %></l1>
            <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to 'Join', new_user_registration_path %></li>
            <li><%= link_to 'Sign in ', new_user_session_path %></li>
          <% end %>
        </ul>
      </div>
    </nav>
</div>
