<%= form_for(@host) do |f| %>
  <% if @host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host.errors.count, "error") %> prohibited this host from being saved:</h2>

      <ul>
      <% @host.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :host_title %><br>
    <%= f.text_field :host_title %>
  </div>
  <div class="field">
    <%= f.label :host_bio %><br>
    <%= f.text_area :host_bio %>
  </div>

  <div class="field">
    <%= f.label :host_avatar %>
    <%= f.file_field :host_avatar %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
